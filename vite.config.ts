import { defineConfig } from "vite";

export default defineConfig({
  // Relative asset paths: the build runs from any mount point
  // (vibing.at/mythos, a root domain, file://, ...).
  base: "./",
});
